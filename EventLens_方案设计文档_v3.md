# EventLens：面向上市公司的事件驱动智能识别、可信评估与脉络追踪系统
## 技术方案 v3.0

> 第五届中国研究生金融科技创新大赛 · 赛题12 · 华泰证券
>
> 设计原则：**可交付、可评测、可泛化**；以 MVP 打底，以模块化预留扩展空间。

---

## v3.0 更新说明

相较 v2.0，本版本新增第10章《关键决策记录与实验验证计划》，对方案中10个最关键的技术决策逐一补充：决策背后的第一性原理推导（区别于"业界常见做法"式的理由）、备选方案的量化对比、可执行的验证实验设计（含成功阈值）、以及"符合预期"与"不符合预期"两条分支下的具体应对——分别对应继续推进、问题诊断、备用方案、进阶方案。第3章相应位置增加了指向第10章对应决策条目的引用，便于阅读时对照。

---

## 目录

1. [整体定位与边界](#1-整体定位与边界)
2. [系统架构总览](#2-系统架构总览)
3. [模块详细设计](#3-模块详细设计)
   - 3.1 数据获取层
   - 3.2 文档解析层
   - 3.3 数据清洗与低质内容识别
   - 3.4 事件识别与要素抽取
   - 3.5 同源事件聚合
   - 3.6 事件知识图谱持久化层
   - 3.7 可信度评估
   - 3.8 脉络追踪与时间线
   - 3.9 预警输出
4. [数据策略](#4-数据策略)
5. [技术选型](#5-技术选型)
6. [评测方案](#6-评测方案)
7. [开发优先级与里程碑](#7-开发优先级与里程碑)
8. [泛化设计与扩展空间](#8-泛化设计与扩展空间)
9. [已知风险与应对](#9-已知风险与应对)
10. [关键决策记录与实验验证计划](#10-关键决策记录与实验验证计划)

---

## 1 整体定位与边界

### 1.1 系统一句话描述

> 面向上市公司舆情、公告、政策等多源金融文本，构建**获取 → 解析 → 识别 → 抽取 → 聚合 → 持久化 → 评信 → 追踪 → 预警**全链路事件智能分析系统，输出带证据链的可解释预警结论。

### 1.2 做什么（MVP 范围）

| 任务 | 交付物 |
|---|---|
| 多源数据自动获取（可选启用） | 标准化原始文本/PDF 队列 |
| 多源文本统一解析 | 清洗后的结构化文本 JSON |
| 低质内容识别与过滤 | 质量标签 + 过滤后语料 |
| 事件类型识别 + 情感方向 | 分类标签 + 置信度 |
| 事件要素抽取 | 主体、时间、影响对象、影响方向、证据句 |
| 同源事件聚合与去重 | 事件簇 + 代表性文章 |
| 事件知识图谱持久化 | 可查询、可回溯的事件关系图谱 |
| 可信度量化评估 | 0~1 评分 + 可解释分项得分 |
| 事件脉络时间线 | 带阶段标注的时序事件链 |
| 分级预警推送 | 风险等级（可信度×严重性）+ 推送理由 + 证据来源 |

### 1.3 不做什么（第一版边界）

- 不做股价涨跌预测，不做投资建议
- 不做完整产业链传导推理（图谱仅做事件级关联，不做财务建模式的传导计算）
- 不做无来源的自由生成分析报告
- 不做大模型全量微调（标注数据不足）
- 不做毫秒级实时流式处理（采用定时批拉取 + 增量处理，非真流式）

---

## 2 系统架构总览

```
┌──────────────────────────────────────────────────────────────────┐
│                  数据获取层（新增，可配置启用）                    │
│   akshare/tushare 公告接口 │ 舆情新闻接口 │ 定时调度 │ 人工导入兜底 │
└────────────────────────┬─────────────────────────────────────────┘
                         │
┌────────────────────────▼─────────────────────────────────────────┐
│                      文档解析层                                   │
│   MinerU PDF→Markdown  │  Qwen2.5-VL 扫描件识别  │ 结构化字段提取  │
└────────────────────────┬─────────────────────────────────────────┘
                         │
┌────────────────────────▼─────────────────────────────────────────┐
│              数据清洗 + 低质内容识别层（细化）                     │
│   规则信号(标题党/黑白名单) │ 统计信号(来源历史) │ LLM可信度信号    │
│   公司实体归一 │ 时间解析                                          │
└────────────────────────┬─────────────────────────────────────────┘
                         │
          ┌──────────────┴──────────────┐
          │                             │
┌─────────▼──────────┐     ┌────────────▼───────────┐
│   事件理解层        │     │   向量索引层            │
│ 类型分类(小模型)    │     │ BAAI/bge-m3 编码       │
│ 要素抽取(Qwen3)    │     │ FAISS/Milvus 存储      │
│ 证据句识别          │     │ 增量更新支持            │
│ 置信度估计          │     └────────────┬───────────┘
└─────────┬──────────┘                  │
          └──────────────┬──────────────┘
                         │
┌────────────────────────▼─────────────────────────────────────────┐
│                      同源聚合层                                   │
│   语义相似度 + 主体一致性 + 时间窗口 + 事件类型约束               │
│   LLM 共指推理（可选增强）  │  事件簇生成与增量更新               │
└────────────────────────┬─────────────────────────────────────────┘
                         │
┌────────────────────────▼─────────────────────────────────────────┐
│              事件知识图谱持久化层（新增）                          │
│   Company/Event/Article/Regulator 节点 │ 双时态边(t_valid/t_invalid)│
│   SAME_EVENT 合并 │ FOLLOWED_BY 承接 │ RELATED_TO 历史关联         │
└────────────────────────┬─────────────────────────────────────────┘
                         │
┌────────────────────────▼─────────────────────────────────────────┐
│                      可信评估层                                   │
│   来源权威性 │ 多源一致性 │ 官方佐证 │ 图谱历史来源可信度回填      │
└────────────────────────┬─────────────────────────────────────────┘
                         │
┌────────────────────────▼─────────────────────────────────────────┐
│                      脉络追踪层                                   │
│   时间线构建 │ 阶段识别 │ 事件反转检测 │ 官方回应融合              │
└────────────────────────┬─────────────────────────────────────────┘
                         │
┌────────────────────────▼─────────────────────────────────────────┐
│              输出应用层（预警机制重构）                            │
│   可信度×严重性二维分级 │ 推送节流去重 │ 事件冷却归档              │
│   事件摘要 │ 时间线 │ 证据来源 │ 推送理由                          │
└──────────────────────────────────────────────────────────────────┘
```

---

## 3 模块详细设计

### 3.1 数据获取层

*选型依据见 §10.10*

**问题**：人工输入只能应付一次性静态数据集，无法支撑系统持续运行，也无法体现系统的工程完整性。

**方案**：以官方/合规数据接口为主，人工导入为兜底，不做自建爬虫。

| 数据类型 | 接口来源 | 说明 |
|---|---|---|
| 上市公司公告 | tushare `anns_d` 接口 | 提供 PDF 下载 URL，单次最大 2000 条，按日期循环获取全量 |
| 上市公司公告（备选） | akshare 公告类接口 | 对接交易所/巨潮资讯网，数据相对权威 |
| 滚动财经新闻 | tushare `get_latest_news` | 含分类、标题、链接、正文，可定时拉取 |
| 行情/基础信息 | akshare `stock_zh_a_hist` 等 | 用于公司实体归一的基础字典（代码-名称-行业映射） |

**调度设计**：

```python
# 定时任务伪代码框架
import akshare as ak
import tushare as ts

def fetch_pipeline():
    # 公告类：官方权威源，入队即标注 source_authority=1.00
    announcements = pro.anns_d(ann_date=today)
    queue.push(announcements, source_type="official")

    # 舆情类：入队后走完整清洗 + 低质过滤流程（见 3.3）
    news = ts.get_latest_news(top=200, show_content=True)
    queue.push(news, source_type="media")
```

公告类数据按交易日定时拉取（交易所披露有固定时点，建议每小时一次）；舆情类数据建议 15~30 分钟一次，并做 URL/标题去重缓存，避免重复处理同一篇文章。

**合规说明**：akshare/tushare 官方文档明确标注其接口基于公开数据源封装，仅限学术研究使用，需控制调用频率，由此产生的风险由使用者自行承担。比赛和演示场景下使用无实质障碍，但工程上应保留人工/批量文件导入接口作为兜底，避免单点依赖外部接口的可用性。

**与现有比赛数据集的关系**：比赛提供的 2000 条标注 + 20 万条无标签数据是静态训练/评测语料，数据获取层不替代这部分，而是作为系统在比赛之外、面向真实场景持续运行时的输入通道，体现系统的可部署性。

---

### 3.2 文档解析层

*选型依据见 §10.9*

**问题**：赛题数据包含舆情文本（纯文字）和上市公司公告（PDF，含扫描件）。直接 cat PDF 会乱码，需要统一解析。

**方案**：

| 文档类型 | 工具 | 输出 |
|---|---|---|
| 有文字层的 PDF（公告、研报） | MinerU 3.x | Markdown + 表格 JSON |
| 扫描件 / 图片型 PDF | Qwen2.5-VL-7B | 直接图像→文本 |
| 纯文本舆情 | 直接读取 | 保持原格式 |
| Excel 数据字段 | openpyxl / pandas | 结构化 DataFrame |

**MinerU 接入方式**（Python）：
```python
from mineru import MinerU
parser = MinerU()
result = parser.parse("announcement.pdf")  # 返回 Markdown + JSON
```

**路由逻辑**：先用 `pdfinfo` 检查是否有文字层，有则走 MinerU，无则走 Qwen2.5-VL。

**扩展空间**：后续可以接入更专门面向金融文档的解析模型，或升级 MinerU2.5-Pro 获得更强的跨页表格合并能力。

---

### 3.3 数据清洗与低质内容识别

*选型依据见 §10.5*

#### 3.3.1 设计原则：三层信号融合，而非单一方法

低质内容识别不依赖单一手段，而是规则信号、统计信号、LLM 生成信号三层叠加，各自负责不同精度/成本区间的判断，最终汇入统一的质量分。

```
全部输入文章
  ├─ [第一层] 规则信号（毫秒级，覆盖全量数据）
  │     标题党特征词 │ 来源黑白名单 │ 内容长度异常
  │     无署名/无信源措辞("据传""网传") │ 转载链路异常
  │
  ├─ [第二层] 统计信号（基于20万条语料离线挖掘）
  │     来源历史"被辟谣率" │ 首发率 vs 转载率
  │     传播曲线模式（正常衰减 vs 异常爆发后消失）
  │
  └─ [第三层] LLM 生成可信度信号（仅对前两层无法判定的样本调用）
        让 LLM 对一组明确信号分别打分，而非直接输出"真/假"判断
```

#### 3.3.2 第一层：规则信号

| 信号 | 判断方式 |
|---|---|
| 标题党特征 | 夸张词/悬念式标题 + 正文与标题内容不匹配 |
| 来源可信名单 | 命中预构建的来源等级表（见 3.7） |
| 无信源措辞 | 正则匹配"据传""网传""消息人士透露"等模式 |
| 转载链路异常 | 短时间窗口内大量结构高度相似的转发 |

#### 3.3.3 第二层：统计信号（20万条语料挖掘产出）

- 每个来源的历史"被辟谣率"：该来源发布的内容后续被官方/权威来源证伪的比例
- 首发率：该来源历史上作为某事件最早报道者的占比（越高说明越接近一手信息）
- 传播曲线异常检测：短时间内爆发式传播后迅速消失，通常对应刷量或带节奏行为，而非真实事件扩散

这一层产出是**持续更新的来源画像**，不是静态规则，需要随数据积累动态调整。

#### 3.3.4 第三层：LLM 生成可信度信号（关键设计）

不让 LLM 直接输出"真/假"二元判断（这类主观打分不可复现、不可解释），而是让 LLM 针对明确定义的、可解释的信号分别打分，信号本身是结构化的，组合逻辑仍由规则完成：

```python
SIGNAL_PROMPT = """
请针对以下文章，分别评估以下信号（每项 0-1 打分，不要给出真假结论）：
1. 是否包含具体可验证的细节（如日期、文号、金额）
2. 是否存在情绪化/煽动性措辞
3. 逻辑是否自洽，有无内部矛盾
4. 是否有可追溯的信息来源

文章：{content}

输出 JSON：
{
  "concrete_detail_score": 0.0-1.0,
  "emotional_language_score": 0.0-1.0,
  "logical_consistency_score": 0.0-1.0,
  "traceable_source_score": 0.0-1.0
}"""
```

这些信号再以固定权重并入第一、二层规则，形成最终质量分，判断逻辑全程透明，避免黑盒结论。

#### 3.3.5 与可信度评估模块的关系

低质内容识别是**前置过滤**（决定一篇文章是否进入后续处理流程），可信度评估（3.7节）是**后置评分**（决定一个已聚合事件簇的可信程度）。两者使用部分相同的信号（来源权威性、传播模式），但服务于不同阶段，不应混为一谈。

---

### 3.4 事件识别与要素抽取

#### 3.4.1 架构设计：两阶段分工

```
文章文本（已通过低质过滤）
  ├─ [阶段1] 事件类型分类器（小模型，快）
  │     └─ 判断文章是否含有事件，输出事件类型标签 + 置信度
  └─ [阶段2] 要素抽取（LLM，准）
        └─ 仅对阶段1置信度 > 阈值的文章调用，节省推理成本
```

#### 3.4.2 阶段1：事件类型分类器

*选型依据见 §10.1*

**为什么训练小模型而不直接用LLM**：20万条数据需要批量处理，LLM推理成本高；小模型做粗筛，LLM做精抽，各司其职。

**输入**：标题 + 正文前512字 + 实体字段 + 来源权重

**模型选型**：
- Baseline：`BAAI/bge-large-zh-v1.5` 编码 + LightGBM 分类
- 提升版：`Qwen2.5-7B-Instruct` 做 few-shot 分类（无需微调）
- 备选：对2000条标注数据做 LoRA 微调 `Qwen2.5-3B`

**伪标签扩增流程**（利用20万无标签数据）：
```
2000条标注 → 训练初始分类器
    → 预测20万条，保留置信度 > 0.9 的样本
    → 每类限额（防止热门类淹没长尾类），控制类别均衡
    → 合并扩增后重训练
    → 在保留的纯人工验证集上验证提升效果
```

**输出 JSON**：
```json
{
  "article_id": "A001",
  "has_event": true,
  "event_type": "监管处罚",
  "event_polarity": "负面",
  "classifier_confidence": 0.91
}
```

#### 3.4.3 阶段2：要素抽取（Qwen3 Structured Output）

*选型依据见 §10.2*

**核心思想**：不训练序列标注模型，直接用 Qwen3 约束 JSON schema 输出。

**Prompt 设计（Schema-constrained）**：
```python
SYSTEM_PROMPT = """你是专业的金融事件要素抽取专家。
从给定文章中识别事件要素，严格按 JSON 格式输出，不得添加额外说明。
若某字段无法从文章中确认，输出 null，不要猜测。"""

USER_PROMPT = """
文章信息：
标题：{title}
来源：{source}
日期：{date}
正文：{content}

请抽取以下要素（JSON格式）：
{
  "event_subject": "事件主体公司（若缺失则null）",
  "event_type": "事件类型",
  "event_time": "事件发生时间（ISO格式，若模糊则null）",
  "impact_target": "影响对象",
  "impact_direction": "正面/负面/中性",
  "impact_description": "影响描述（简洁）",
  "evidence_sentence": "最关键的证据句（原文摘录，不超过80字）",
  "is_official_source": true/false,
  "extraction_confidence": 0.0-1.0
}"""
```

**接入方式**：
```python
from openai import OpenAI
client = OpenAI(base_url="https://dashscope-intl.aliyuncs.com/compatible-mode/v1")

response = client.chat.completions.create(
    model="qwen3-max",   # 或 qwen-max-2025-01-25
    messages=[
        {"role": "system", "content": SYSTEM_PROMPT},
        {"role": "user", "content": USER_PROMPT.format(**article)}
    ],
    response_format={"type": "json_object"}  # 强制 JSON 输出
)
```

**规则兜底**：对 `extraction_confidence < 0.6` 的结果，用正则+关键词规则补全缺失字段（时间、公司名等强结构化字段）。

**扩展空间**：
- 当有更多标注数据后，可以对 Qwen2.5-7B 做 LoRA 微调，进一步提升特定事件类型的抽取精度
- 可以借鉴对比学习思路，用 LLM 对2000条样本做多模式改写扩充训练数据
- 后续可以替换为更强的模型版本

---

### 3.5 同源事件聚合

*选型依据见 §10.3*

#### 3.5.1 问题本质

同一事件被不同媒体用不同表述报道，需要识别并聚合。这本质是**事件共指消解（Event Co-reference Resolution）**的工程化实现。

#### 3.5.2 多因子加权相似度

```
event_similarity =
    0.35 × 文本语义相似度（FAISS cosine）
  + 0.25 × 主体公司一致性（实体归一后精确匹配）
  + 0.15 × 事件类型一致性（分类结果匹配）
  + 0.15 × 时间窗口接近度（高斯衰减，时间差越大得分越低）
  + 0.10 × 关键实体重合度（证据句中命名实体的 Jaccard）
```

**聚合流程**：
```python
def cluster_events(articles, threshold=0.72):
    # 1. 向量化所有文章（标题+证据句加权）
    embeddings = bge_model.encode([a['title'] + a['evidence_sentence'] for a in articles])
    
    # 2. FAISS 召回候选对
    index = faiss.IndexFlatIP(dim)
    index.add(embeddings)
    
    # 3. 多因子精排
    pairs = get_candidate_pairs(index, top_k=20)
    filtered_pairs = [(a, b) for a, b in pairs 
                      if multi_factor_similarity(a, b) > threshold]
    
    # 4. Union-Find 生成事件簇
    clusters = union_find(filtered_pairs)
    return clusters
```

#### 3.5.3 LLM 共指推理增强（可选高阶功能）

对于多因子得分在 `[0.60, 0.72]` 的边界对，调用 LLM 做语义推理判断，同时获得可解释的判断依据：

```python
COREF_PROMPT = """
以下两篇文章是否在描述同一个金融事件？
文章A：{article_a_summary}
文章B：{article_b_summary}
请先给出"是"或"否"的判断，再用一句话解释原因。
输出格式：{"is_same_event": true/false, "reason": "..."}"""
```

这个功能既提升召回准确率，又为聚合结果提供可解释依据，在演示时效果显著。

**输出事件簇**：
```json
{
  "event_cluster_id": "EC-2026-0412-001",
  "main_company": "示例科技股份有限公司",
  "event_type": "监管处罚",
  "start_time": "2026-04-10",
  "latest_time": "2026-04-12",
  "article_count": 7,
  "article_ids": ["A001", "A027", "A391", "A445", "A502", "A618", "A703"],
  "representative_article_id": "A027",
  "representative_evidence": "公司因信息披露违规收到证监会处罚决定书。",
  "is_new_push": true,
  "cluster_confidence": 0.88
}
```

事件簇生成后，进入知识图谱持久化层（3.6节），不再是孤立的批处理结果。

---

### 3.6 事件知识图谱持久化层

*存储方案选型依据见 §10.6，双时态建模依据见 §10.7*

#### 3.6.1 为什么需要这一层

如果事件簇只是批处理产出的临时结果，系统就无法回答"这次事件和该公司过去的类似事件有什么关系""市场在某个时间点已知道什么信息"这类问题。需要一个持久化、可查询、带时间维度的知识图谱，让事件之间真正建立联系，而不是各自孤立存档。

#### 3.6.2 节点与边的设计

**节点类型**：

| 节点类型 | 关键属性 |
|---|---|
| Company | 公司代码、名称、行业、别名 |
| Event | 事件类型、当前阶段、可信度、严重性 |
| Article/Mention | 来源、原文链接、证据句、发布时间 |
| Person | 高管/实控人姓名、职务 |
| Regulator/Source | 监管方或权威来源标识 |

**边类型**：

| 边类型 | 连接 | 说明 |
|---|---|---|
| SUBJECT_OF | Company → Event | 公司是该事件的主体 |
| MENTIONS | Article → Event | 文章报道了该事件 |
| ISSUED_BY | Event → Regulator | 事件有官方文件佐证 |
| FOLLOWED_BY | Event → Event | 同一事件链的时序承接（阶段演进） |
| RELATED_TO | Event → Event | 与历史相似事件关联，但不属于同一事件 |
| TRIGGERED | Event → Event | 跨公司因果关联（如供应链上下游传导，第一版仅做关联标注，不做传导计算） |

公司间静态关系（供应链、控股、竞对）可直接从 akshare/tushare 的行业分类、股东数据预构建，不依赖事件抽取流程，作为图谱的静态底座。

#### 3.6.3 双时态建模：解决"持久化"的核心问题

每条边携带显式的有效期区间 `(t_valid, t_invalid)`，记录"这个事实从何时起成立、何时失效"，而不是简单覆盖旧状态。这样可以回答"4月11日时，市场已知道什么"这类回溯查询，同一事件在不同时间点的可信度演变也被完整保留，而非被最新状态覆盖。

```json
{
  "edge_type": "SUBJECT_OF",
  "from": "Event:EC-2026-0412-001",
  "to": "Company:示例科技",
  "t_valid": "2026-04-10T09:00:00+08:00",
  "t_invalid": null,
  "credibility_at_creation": 0.62,
  "credibility_current": 0.97
}
```

#### 3.6.4 新事件如何与历史建立联系

```python
def link_to_graph(new_article, company_node):
    # 1. 双路召回：向量相似度 + 图遍历（不调用 LLM，控制延迟）
    candidates = vector_recall(new_article, company_node, top_k=10) \
               + graph_traverse(company_node, event_type=new_article.event_type)

    for candidate_event in candidates:
        score = multi_factor_similarity(new_article, candidate_event)

        if score > 0.72 and within_time_window(new_article, candidate_event):
            # 同一事件的延续，合并入簇，更新时间线
            merge_and_create_edge(new_article, candidate_event, "FOLLOWED_BY")
            return candidate_event.id

        elif score > 0.45:
            # 历史相似但非同一事件，建立关联边供分析师参考
            create_edge(new_article, candidate_event, "RELATED_TO")

    # 都不满足，新建孤立事件节点
    return create_new_event_node(new_article)
```

**三种结果**：语义高度重合且时间窗内 → `SAME_EVENT` 合并；语义中等但同公司同类型（如本次"股权质押"历史上曾导致评级下调）→ `RELATED_TO`，不合并但呈现为"历史相似先例"；完全不相关 → 新建节点。

#### 3.6.5 图数据库选型

开发阶段用 NetworkX 足够轻量，便于快速迭代；演示/生产阶段建议上 Neo4j，Cypher 查询时间线和关联事件直观，且有成熟的可视化插件，适合答辩展示。

#### 3.6.6 与其他模块的反哺关系

知识图谱不是终点模块，而是反哺前后两端：向前反哺 3.7 可信度评估（历史来源可信度可以直接从图谱里该来源过往边的 `credibility_current` 统计得出，不需要重新计算）；向后支撑 3.8 脉络追踪（时间线本质就是对 `FOLLOWED_BY` 边链的遍历输出）。

---

### 3.7 可信度评估

*选型依据见 §10.4*

#### 3.7.1 设计原则

**不用 LLM 主观打分**。可信度评估必须基于可解释的规则体系，原因：
1. LLM 主观打分不可重复、不可解释
2. 规则系统对评审更友好（可以清晰展示评分逻辑）
3. 规则可以在不重训练的情况下快速调整

3.3 节中 LLM 生成的结构化信号（具体细节得分、情绪化措辞得分等）可以作为本模块的输入特征之一，但信号的组合方式仍然是固定规则，而非 LLM 直接输出最终分数。

#### 3.7.2 评分体系

**来源权威性评分**（预先构建来源等级表，可从知识图谱历史数据持续校准）：

| 来源类型 | 权威性得分 | 示例 |
|---|---|---|
| 交易所公告、监管文件 | 1.00 | 上证所、深交所、证监会 |
| 上市公司官方公告 | 0.90 | 公司官网、年报 |
| 主流财经媒体 | 0.75 | 证券时报、上海证券报、财新 |
| 综合新闻媒体 | 0.55 | 新华社、人民日报财经版 |
| 财经自媒体/分析平台 | 0.35 | 雪球、东方财富评论 |
| 低质来源/无来源转载 | 0.15 | 无法溯源的转发 |

**综合可信度公式**：
```
credibility_score =
    0.30 × source_authority_score         # 来源权威性
  + 0.25 × multi_source_consistency       # 多独立来源一致性（不含转载）
  + 0.25 × official_endorsement           # 官方文件佐证（0或1）
  + 0.10 × content_completeness           # 要素完整度
  + 0.10 × recency_score                  # 时效性（近期更高）
  - duplicate_penalty                     # 大量转载但无独立来源：扣分
  - conflict_penalty                      # 存在反向证据/官方澄清：扣分
```

**扩展空间**：
- 来源权威性表不再是静态维护，而是定期从知识图谱里该来源历史事件的最终可信度分布中重新统计校准
- 接入传播路径分析（异常短时间内大量转发通常是放大噪音而非高可信）

---

### 3.8 脉络追踪与时间线

#### 3.8.1 阶段识别

对同一事件簇内的文章按时间排序（即遍历知识图谱中的 `FOLLOWED_BY` 边链），用 LLM 对每篇文章分配阶段标签：

```
媒体首发 → 多源扩散 → 公司回应 → 监管问询 → 官方确认 → 反转/澄清 → 后续影响
```

**阶段识别规则**（规则优先，LLM辅助）：

| 阶段 | 规则触发条件 |
|---|---|
| 媒体首发 | 事件簇中时间最早的文章 |
| 官方确认 | 来源为交易所/监管 OR 包含"公告"、"披露"等关键词 |
| 监管问询 | 包含"问询函"、"关注函"、"监管函" |
| 公司回应 | 来源为公司公告 AND 包含"回复"、"说明"、"澄清" |
| 事件反转 | 包含"撤销"、"澄清"、"不属实"且可信度高 |

#### 3.8.2 时间线输出格式

```json
{
  "event_cluster_id": "EC-2026-0412-001",
  "company": "示例科技股份有限公司",
  "event_type": "监管处罚",
  "timeline": [
    {
      "date": "2026-04-10",
      "stage": "媒体首发",
      "summary": "多家媒体报道公司疑似存在信息披露违规问题。",
      "source_count": 3,
      "max_credibility": 0.62,
      "evidence_article_id": "A001"
    },
    {
      "date": "2026-04-11",
      "stage": "多源扩散",
      "summary": "报道来源扩大至12家媒体，事件引发市场关注。",
      "source_count": 12,
      "max_credibility": 0.71,
      "evidence_article_id": "A027"
    },
    {
      "date": "2026-04-12",
      "stage": "官方确认",
      "summary": "深交所发布问询函，公司收到监管关注。",
      "source_count": 2,
      "max_credibility": 0.97,
      "evidence_article_id": "A391"
    }
  ],
  "current_stage": "官方确认",
  "is_resolved": false,
  "related_historical_events": [
    {"event_cluster_id": "EC-2024-1102-007", "relation": "RELATED_TO", "summary": "2024年同公司因类似问题被出具问询函"}
  ]
}
```

---

### 3.9 预警输出（机制重构）

*选型依据见 §10.8*

#### 3.9.1 核心修正：可信度与严重性必须分离

v1.0 中风险等级直接由可信度单一维度决定，这是一个结构性缺陷：可信度回答的是"这事有多大把握是真的"，严重性回答的是"这事有多糟"。一个停车违规公告可信度可以是1.0（官方发布，确凿无疑），但严重性极低；一个财务造假传闻可信度可能只有0.5，但一旦坐实严重性极高，两者不应压缩成一个数字。

**严重性评分**（基于事件类型预设基准，可人工调整）：

| 事件类型示例 | 严重性基准分 |
|---|---|
| 财务造假、退市风险 | 0.90 ~ 1.00 |
| 监管处罚、重大诉讼 | 0.65 ~ 0.85 |
| 高管变动、股权质押 | 0.35 ~ 0.55 |
| 常规公告、日常经营信息 | 0.05 ~ 0.20 |

**可信度 × 严重性二维矩阵**：

| | 严重性低 | 严重性高 |
|---|---|---|
| **可信度高** | 常规披露，正常推送，不升级关注 | 高优先级预警，立即推送 |
| **可信度低** | 不推送，归档观察 | 标注"待核实"重点关注，这正是预警系统帮分析师过滤噪音价值最大的场景，绝不能简单按可信度过滤掉 |

#### 3.9.2 预警节流与去重机制

同一事件簇持续发酵会反复产生新文章，若每篇都推送会造成预警疲劳。规则：除非满足以下条件之一，否则不重复推送，仅在已有预警下追加"最新进展"记录：

- 事件阶段发生跃迁（如从"媒体首发"进入"官方确认"）
- 可信度跨越关键阈值（如从 0.6 升至 0.8 以上）
- 严重性因新证据被重新评估而显著变化

#### 3.9.3 事件冷却与归档机制

事件簇超过设定周期（建议14天）无新文章，且未被官方确认或证伪，自动标记为 `dormant`，从主预警队列移除，但保留在知识图谱中供历史检索与未来相似事件关联（即 3.6 节的 `RELATED_TO` 边）。

#### 3.9.4 输出结构（对应赛题任务4）

```json
{
  "alert_id": "ALT-20260412-001",
  "alert_time": "2026-04-12T14:30:00+08:00",
  "risk_level": "高风险",
  "risk_matrix_position": {"credibility_tier": "高", "severity_tier": "高"},
  "event_cluster_id": "EC-2026-0412-001",
  "company": "示例科技股份有限公司",
  "event_type": "监管处罚",
  "event_summary": "公司因信息披露违规于2026年4月12日收到深交所问询函，事件持续发酵，已有官方确认。",
  "credibility_score": 0.91,
  "severity_score": 0.78,
  "credibility_breakdown": {
    "source_authority": 0.95,
    "multi_source_consistency": 0.88,
    "official_endorsement": 1.00,
    "content_completeness": 0.82,
    "recency_score": 0.95
  },
  "evidence_sources": [
    {"article_id": "A391", "source": "深交所官网", "date": "2026-04-12", "type": "官方文件"},
    {"article_id": "A027", "source": "证券时报", "date": "2026-04-11", "type": "主流媒体"}
  ],
  "push_reason": "事件由官方文件佐证（深交所问询函），可信度极高；事件类型严重性中高，建议重点关注。",
  "timeline_stage": "官方确认",
  "related_article_count": 7,
  "is_throttled_update": false,
  "dormant": false
}
```

---

## 4 数据策略

### 4.1 20万无标签数据的利用策略

这是与其他团队拉开差距的关键，不能浪费。

| 用途 | 具体操作 | 产出 |
|---|---|---|
| 来源权威性基准 | 统计各来源网站的发布频率、被官方引用频率 | 来源等级字典 |
| 伪标签扩增 | 高置信分类结果加入训练，限制每类上限 | 扩充后的训练集 |
| 全文向量索引 | bge-m3 编码所有文章 | FAISS 索引文件 |
| 低质内容挖掘 | 统计转载链路、标题党特征、来源历史辟谣率 | 三层低质过滤信号库 |
| 难例构造 | 挑选边界样本作为内部测试难例 | 难例评测集 |
| 事件时序分布 | 分析事件在时间轴上的分布，了解爆发模式 | 数据分析报告 |

### 4.2 伪标签扩增注意事项

- 验证集必须保留为**纯人工标注**，绝不混入伪标签
- 每类别伪标签数量上限 = `min(2000, 原始该类数量 × 5)`
- 伪标签置信度阈值建议 ≥ 0.90（宁缺勿滥）
- 扩增后在验证集上必须验证 F1 有提升才采用

### 4.3 公开数据集参考

| 数据集 | 用途 | 获取方式 |
|---|---|---|
| ChFinAnn (Doc2EDAG) | 中文金融公告事件抽取基准测试 | GitHub dolphin-zs/Doc2EDAG |
| DuEE-Fin (百度) | 金融事件抽取 baseline 参考 | 百度飞桨平台 |
| CFinDEE (WWW 2024) | 2020-2024细粒度金融事件标注 | 论文附带数据集 |
| FinGLM 年报数据 | 11588份上市公司年报 | GitHub MetaGLM/FinGLM |

---

## 5 技术选型

### 5.1 模型与基础设施选型

| 模块 | 选型 | 理由 |
|---|---|---|
| 数据获取 | akshare / tushare | 官方/合规数据接口，覆盖公告与舆情 |
| 文档解析（有文字层PDF） | MinerU 3.x | 开源最强中文文档解析，支持跨页表格 |
| 文档解析（扫描件） | Qwen2.5-VL-7B | 金融文档结构化输出能力强 |
| 文本向量化 | BAAI/bge-m3 | 多语言，支持中文金融文本，1024维 |
| 事件分类（小模型） | bge-large-zh + LightGBM | 快速，可在 CPU 上批量推理 |
| 事件要素抽取（LLM） | Qwen3-Max（API）/ qwen2.5-72b（本地） | Structured output原生支持，中文能力强 |
| 向量检索 | FAISS（开发） / Milvus（生产） | FAISS 轻量，Milvus 支持增量更新 |
| 知识图谱存储 | NetworkX（开发）/ Neo4j（演示/生产） | Neo4j 支持 Cypher 查询时间线，可视化成熟 |
| 实体归一 | 自建公司名词典 + 模糊匹配 | 控制成本，避免引入额外模型依赖 |

每项选型的完整对比依据见第10章。

### 5.2 工程框架

```
语言：Python 3.11+
数据处理：pandas, polars（大表格）
向量：faiss-cpu / pymilvus
图谱：networkx（开发）/ neo4j-driver（生产）
数据获取：akshare, tushare
LLM 接入：openai SDK（兼容 Qwen API）
文档解析：mineru, PyMuPDF4LLM（备用）
API 服务：FastAPI
演示界面：Streamlit（快速原型）
配置管理：pydantic-settings + .env
```

### 5.3 推理成本控制策略

- 事件分类用小模型粗筛，只有置信度 > 0.7 的文章才调用 LLM 精抽（约节省 60~70% 的 API 调用）
- LLM 调用使用 `qwen-turbo` 做初筛，`qwen-max` 做高置信精抽
- 结果缓存：相同 article_id 的抽取结果缓存到 SQLite，避免重复调用
- 知识图谱召回阶段不调用 LLM（纯向量+图遍历），仅边界案例才触发 LLM 共指推理

---

## 6 评测方案

### 6.1 离线指标

| 任务 | 主要指标 | 备注 |
|---|---|---|
| 事件类型识别 | Macro-F1, Accuracy | 关注长尾类别 F1 |
| 情感/影响方向 | F1 | 三分类：正/负/中性 |
| 事件要素抽取 | 字段级 P/R/F1 | 主体、时间、影响对象各自评分 |
| 同源事件聚合 | Pairwise F1, NMI, ARI | Pairwise F1 最直观 |
| 重复性判断 | Accuracy, F1 | 对应赛题数据的 `重复性标志` 字段 |
| 低质内容识别 | Precision/Recall（人工标注子集） | 重点关注误杀率（高质量内容被错误过滤） |
| 可信度评估 | 与人工打分 Spearman 相关性 | 需要人工构造100条评测样本 |
| 预警结果 | Top-K 命中率 + 二维矩阵分类准确率 | 人工评审20个预警案例 |

### 6.2 泛化测试设计（关键）

普通随机切分会导致分数虚高。必须设计以下切分方式：

| 切分策略 | 描述 | 目的 |
|---|---|---|
| 时间切分 | 前80%日期训练，后20%日期测试 | 模拟B榜真实时序数据 |
| 公司切分 | 部分公司只在测试集出现 | 测试未知公司的泛化 |
| 来源切分 | 测试集包含训练集未见过的来源网站 | 测试来源泛化 |
| 事件簇切分 | 同源事件不跨训练/验证 | 防止信息泄露导致分数虚高 |

### 6.3 人工评测样本构造

构造200条人工评测样本，覆盖：
- 20条标准正例（事件清晰、证据充分）
- 20条谣言/低质内容（测试可信度过滤）
- 20条边界事件（事件要素不完整）
- 20条同源事件对（测试聚合准确性）
- 20条事件反转案例（测试脉络追踪）
- 20条历史关联案例（测试知识图谱 RELATED_TO 召回准确性）
- 100条随机样本（保证分布覆盖）

---

## 7 开发优先级与里程碑

### 第一阶段：MVP（2周）

目标：跑通完整链路，有可演示的端到端结果。数据获取层和知识图谱在此阶段先用简化版本（人工导入 + NetworkX 内存图）。

```
Week 1:
  Day 1-2  数据字段分析，事件类型分布统计，来源分布统计
  Day 3-4  数据清洗流程（含低质规则信号）+ 公司实体归一 + 时间解析
  Day 5-7  事件分类 baseline（bge + LightGBM）

Week 2:
  Day 8-9  Qwen3 要素抽取 prompt 设计与调优
  Day 10-11 bge-m3 向量索引 + 同源聚合初版
  Day 12-13 可信度评分规则初版 + 时间线输出
  Day 14   Streamlit 演示界面 + 端到端验证
```

**Week 2 结束时必须能做到**：
- 输入一批文章，输出完整预警 JSON
- 有3~5个真实上市公司事件案例可以演示

### 第二阶段：优化（2周）

```
Week 3:
  伪标签扩增 + 分类器提升
  MinerU 接入公告 PDF 解析
  同源聚合阈值调优 + 难例分析
  知识图谱 schema 落地（NetworkX → Neo4j 迁移）

Week 4:
  数据获取层接入 akshare/tushare（替换部分人工导入）
  可信度评分细化（来源词典扩充，引入图谱历史校准）
  预警二维矩阵 + 节流/冷却机制实现
  泛化评测（时间切分、公司切分）
  错误分析报告
```

### 第三阶段：打磨（1周）

```
  演示界面优化（事件脉络可视化、知识图谱关系图展示、证据高亮）
  文档撰写：技术报告 + 系统说明书
  消融实验：验证各模块贡献
  准备答辩材料
```

---

## 8 泛化设计与扩展空间

### 8.1 已内置的泛化设计

| 设计决策 | 泛化价值 |
|---|---|
| 事件类型采用赛题提供的事件体系（JSON），不硬编码 | 新增事件类型只需修改配置，无需重训练 |
| 来源权威性基于规则词典，不依赖特定域 | 新增来源只需更新词典 |
| 要素抽取用 LLM + Schema，不用 NER 序列标注 | 新增要素字段只需修改 Prompt Schema |
| 可信度评分为透明规则，权重可调 | 不同业务场景可以调整各项权重 |
| 向量索引与聚合逻辑解耦 | 可替换 embedding 模型而不改变聚合逻辑 |
| 知识图谱 schema 节点/边类型可扩展 | 新增实体类型（如产品、地域）只需扩展节点定义 |
| 数据获取层与处理流程解耦 | 可随时切换/新增数据源，不影响下游模块 |

### 8.2 后续可以扩展的方向

**扩展1：实时流式处理**
当前设计是定时批拉取 + 增量处理。后续可以接入 Kafka，改造为真流式管道。基础设施已经为此预留（向量索引支持增量更新，事件簇支持增量归并，图谱边支持增量写入）。

**扩展2：完整产业链传导推理**
当前知识图谱的 `TRIGGERED` 边仅做跨公司事件关联标注，不做传导强度计算。后续可以在静态供应链关系边的基础上，结合历史事件传导案例，建立传导强度的统计模型。

**扩展3：多模态公告理解**
当前公告解析停留在文字层面。后续可以接入 Qwen2.5-VL 处理公告中的表格图片、财务图表，进一步提升要素抽取完整度。

**扩展4：事件影响量化**
在可信评估基础上，加入历史上同类事件对股价影响的统计分析，给出"参考历史相似事件，短期影响约为 X%"的定量参考。这层不做预测，只做历史统计，规避合规风险，可以直接复用知识图谱中 `RELATED_TO` 边召回的历史相似事件。

**扩展5：大模型微调**
当积累更多人工标注数据（建议500条以上）后，对 `Qwen2.5-7B` 做事件要素抽取的 LoRA 微调，可以大幅降低推理成本（不再依赖 API，可以本地推理）并提升特定事件类型的准确率。

---

## 9 已知风险与应对

| 风险 | 可能性 | 应对策略 |
|---|---|---|
| 伪标签扩增引入噪声，分类器退化 | 中 | 严格保留纯人工验证集，扩增后必须验证F1提升 |
| LLM 要素抽取出现幻觉（编造不存在的内容） | 中 | Prompt 中强调"无法确认则输出null"；用规则校验关键字段是否出现在原文中 |
| 同源聚合过度合并（不同事件被错误归为一类） | 中 | 提高阈值至0.72以上；双向验证主体一致性 |
| 来源权威性词典覆盖不全 | 高 | 预留未知来源的默认分（0.35）；持续从20万数据中挖掘新来源 |
| Qwen API 调用成本超预算 | 中 | 小模型粗筛控制调用量；缓存已处理结果 |
| 赛题B榜数据来源/公司分布与训练集差异大 | 中 | 时间切分+公司切分的内部泛化测试提前暴露此问题 |
| 知识图谱边语义判断错误，导致误连历史事件 | 中 | RELATED_TO 边设置较低阈值仅做呈现参考，不参与自动合并；SAME_EVENT 合并阈值保持严格 |
| 外部数据接口（akshare/tushare）不稳定影响获取 | 中 | 保留人工/批量文件导入兜底通道，接口失败自动降级 |
| 严重性基准分人工设定存在主观性 | 中 | 严重性基准表作为可调配置项独立维护，演示时展示设定依据，而非声称客观真值 |

---

## 10 关键决策记录与实验验证计划

本章对方案中10个最关键的技术决策做系统性记录。每条记录遵循统一框架：先回到问题的第一性原理（即不依赖"业界常见做法"，而是从数据规模、成本约束、可解释性需求等不可再分解的基本事实出发，推导出决策的必然性或排除项），再给出备选方案的量化对比，然后是可执行的验证实验设计（含成功阈值），最后分别说明"符合预期"与"不符合预期"两条分支下的具体动作——分支中又包含诊断方向、备用方案（保底，牺牲部分效果换稳妥）和进阶方案（资源允许时的升级路径）。

### 10.1 事件类型分类器：小模型批处理 vs LLM全量调用 vs LoRA微调

**第一性原理**：判断的本质约束来自三个相互独立的硬事实——待处理数据规模（20万条）、标注数据规模（仅2000条）、LLM单次调用边际成本不为零。三者共同决定了"必须用小模型覆盖全量数据，LLM只能处理少量样本"是结构性结论，而非偏好选择；2000条标注数据量级也不足以支撑一个深度序列模型从零训练，但足够训练一个基于预训练embedding的浅层分类器。

**备选方案对比**：

| 方案 | 准确率预期 | 20万条处理成本 | 工程复杂度 | 结论 |
|---|---|---|---|---|
| bge-large-zh + LightGBM | 中等，依赖2000条数据质量 | 几乎零（本地CPU推理） | 低 | 唯一能覆盖全量数据的方案 |
| Qwen2.5-7B few-shot 全量调用 | 较高 | 数千至上万元，耗时数十小时 | 低 | 成本不可接受，排除 |
| LoRA 微调 Qwen2.5-3B（全量） | 高，但依赖标注质量 | 一次性训练成本 + 本地推理 | 高 | 2000条对3B级别微调仍偏少，过拟合风险高，列为进阶方案 |

**选定方案**：bge+LightGBM 做全量分类，LLM 与 LoRA 微调降级为按需使用。

**验证实验**：从2000条标注中分层抽样划出20%（约400条，保证各类别覆盖）作为纯人工验证集，评估 Macro-F1。符合预期阈值：Macro-F1 ≥ 0.70，且长尾类别 F1 ≥ 0.50。

**符合预期 →** 直接进入伪标签扩增流程，目标将 Macro-F1 提升至0.78以上。

**不符合预期 → 诊断**：查看混淆矩阵，判断是类别不均衡问题还是特征表达不足（embedding 是否适配金融术语）。

**备用方案**：更换为金融领域适配性更强的中文 embedding 模型，或退化为关键词规则+分类器的混合方案，先保精确率、容忍部分召回损失。

**进阶方案**：标注数据积累到500条以上后，对 Qwen2.5-3B 做事件分类专用 LoRA 微调替代当前小模型，预期准确率进一步提升，且仍可本地部署、零边际成本。

---

### 10.2 事件要素抽取：LLM Structured Output vs 序列标注模型

**第一性原理**：要素抽取要求理解"主体""影响方向"这类抽象语义角色，不是简单的token边界分类，这类任务对训练数据量的要求显著高于普通NER（中文NER任务通常需要数千至上万条标注才能稳定收敛）。2000条数据无法支撑专用序列标注模型训练，而预训练LLM已经在海量语料中习得这类语义角色理解能力，通过schema约束即可直接复用，这是"利用已有能力 vs 从零训练"在数据稀缺场景下的必然选择。

**备选方案对比**：

| 方案 | 数据需求 | 抽取准确率预期 | 可控性 | 结论 |
|---|---|---|---|---|
| BERT-CRF 序列标注 | 数千条以上 | 数据不足时偏低 | 高（确定性输出） | 排除，数据量不支持 |
| LLM + JSON Schema（零样本/few-shot） | 几乎不需要标注 | 中高，依赖prompt质量 | 中，需规则校验防幻觉 | 当前选择 |
| LLM + LoRA 微调 | 500条以上效果稳定 | 高 | 中高 | 进阶方案 |

**选定方案**：LLM + JSON Schema 约束输出，2000条数据不足以训练序列标注模型，LLM零样本能力已可用。

**验证实验**：人工核对构造100条要素抽取测试集，分字段计算 Precision/Recall/F1。符合预期阈值：核心字段（主体、影响方向）F1 ≥ 0.80，证据句完全匹配 F1 ≥ 0.60。

**符合预期 →** 进入规则兜底优化阶段，重点处理低置信度样本。

**不符合预期 → 诊断**：先增加3-5个高质量 few-shot 示例排查是否为 prompt 设计问题；若仍不达标，检查字段定义是否存在歧义导致模型理解偏差。

**备用方案**：核心字段（主体识别）单独用实体词典+规则做高精度抽取兜底，LLM仅负责语义性更强的字段。

**进阶方案**：积累500条以上人工核对数据后，对 Qwen2.5-7B 做要素抽取专用 LoRA 微调，降低对 prompt 工程的依赖，支持本地部署。

---

### 10.3 同源事件聚合：多因子加权规则 vs 纯向量相似度 vs 端到端神经网络

**第一性原理**：单一文本向量相似度无法区分"两家不同公司发生的同类型事件"与"同一事件的不同表述"，这是信息论意义上的必然——单一维度信号的互信息不足以唯一确定"同源事件"这个联合判断，必须引入主体、时间等独立维度做联合约束才能消除歧义。

**备选方案对比**：

| 方案 | 歧义消除能力 | 可解释性 | 训练数据需求 | 结论 |
|---|---|---|---|---|
| 纯向量相似度阈值 | 低 | 中 | 无需训练 | 单独使用不可靠，排除 |
| 多因子加权规则 | 高 | 高，每个因子可解释 | 无需训练 | 当前选择 |
| 端到端神经网络共指模型 | 理论最高 | 低（黑盒） | 需要大量同源事件标注对，赛题未提供 | 数据不支持，排除 |

**选定方案**：多因子加权规则，无需专门标注数据，且对评审可解释。

**验证实验**：人工挑选50对已知同源事件文章、50对已知非同源但相似的对抗样本，计算多因子相似度。符合预期阈值：同源对与非同源对相似度均值差距（separation gap）≥ 0.25，阈值0.72处 Precision ≥ 0.85。

**符合预期 →** 固定权重配置，进入大规模聚合。

**不符合预期 → 诊断**：逐因子做消融实验，定位区分度不足的具体因子，针对性调整权重。

**备用方案**：若语义相似度因子区分度不足（bge-m3 对金融术语不敏感），更换为金融领域适配的 embedding 模型。

**进阶方案**：积累500条以上人工确认的同源/非同源标注对后，训练轻量级 LightGBM 二分类模型（输入为当前多因子得分本身），让权重从固定规则变为可学习参数，同时因输入特征仍是可解释因子，保留可解释性。

---

### 10.4 可信度评估：规则化 vs LLM主观打分 vs 训练判别模型

**第一性原理**：可信度评分作为决策依据，核心需求是可复现性——同一篇文章在不同时刻被评估必须得到一致结果。LLM生成具有非确定性（即便固定参数也可能因模型版本/上下文漂移而变化），这与"评分必须稳定可复现"的硬性要求直接冲突，因此不是效果优劣的取舍，而是需求层面的根本不兼容。

**备选方案对比**：

| 方案 | 可复现性 | 可解释性 | 调整灵活性 | 结论 |
|---|---|---|---|---|
| LLM 直接打分 | 低 | 低（黑盒推理过程） | 低 | 排除 |
| 规则化加权公式 | 高 | 高，逐项分解 | 高，权重随时可调 | 当前选择 |
| 训练判别模型 | 高 | 中 | 中，需标注数据重训练 | 进阶方案 |

**选定方案**：规则化加权公式，满足可复现性和可解释性的核心需求。

**验证实验**：构造100条人工评测样本（多人标注取平均），计算系统打分与人工标注的 Spearman 相关系数。符合预期阈值：ρ ≥ 0.6。

**符合预期 →** 固定当前权重配置。

**不符合预期 → 诊断**：对比系统打分与人工打分差异最大的样本，反推哪个分项权重被高估或低估。

**备用方案**：用100条人工标注样本做线性回归拟合最优权重组合，替代经验设定的权重，本质仍是规则化方案，只是权重来源变为数据驱动。

**进阶方案**：积累300条以上人工可信度标注后，训练 LightGBM 预测可信度，输入特征仍是当前规则公式的各分项得分（保留可解释性），但权重组合关系可以是非线性的。

---

### 10.5 低质内容识别：三层信号融合 vs 单一规则 vs 单一LLM

**第一性原理**："低质"本身是多义判断标准（标题党、低权威来源、谣言、纯转载分属不同维度），单一方法在成本和覆盖度上无法兼顾：纯规则成本最低但无法识别语义层面的低质，纯LLM覆盖面最全但20万条全量调用的成本远超预算。三层漏斗式过滤是计算系统中"先快速过滤、再精细判断"的通用原则（类比数据库先索引扫描再精确匹配），在预算约束下是唯一能兼顾覆盖度和精度的结构。

**备选方案对比**：

| 方案 | 20万条处理成本 | 识别精度 | 结论 |
|---|---|---|---|
| 仅规则信号 | 极低 | 中，漏掉语义层面低质 | 召回不足 |
| 仅LLM判断（全量） | 不可接受 | 高 | 成本排除 |
| 三层漏斗（规则→统计→LLM） | 低，LLM只处理少量边界样本 | 高，综合多维信号 | 当前选择 |

**选定方案**：三层漏斗式信号融合，唯一能在预算内覆盖全量数据并保持精度的方案。

**验证实验**：人工标注200条样本（100条已知低质、100条已知正常），评估三层综合后的 Precision/Recall。符合预期阈值：Precision ≥ 0.80（重点关注误杀率），Recall ≥ 0.70。

**符合预期 →** 固化漏斗各层阈值，应用于全量数据。

**不符合预期 → 诊断**：优先检查误杀率是否过高——漏判一条低质内容的代价远小于误杀一条真实预警信息。

**备用方案**：若误杀率过高，调高规则层和统计层的过滤阈值（更保守），让更多样本进入第三层LLM精细判断，以处理成本换精度。

**进阶方案**：三层信号积累到一定标注规模后，训练轻量级融合模型（LightGBM）替代当前固定权重组合，自动学习各层信号的最优组合方式。

---

### 10.6 知识图谱存储：图数据库 vs 关系数据库 vs 文档存储

**第一性原理**：系统核心查询模式是多跳关联查询（公司→历史事件→相关事件→证据文章）。关系数据库处理多跳查询需要多次JOIN，性能随跳数增加急剧下降；文档存储擅长单文档读取但不擅长关系遍历；图数据库的存储模型与图遍历查询模式天然一致，这是"数据结构应匹配访问模式"这一基本工程原则的直接应用，而非追逐技术热点。

**备选方案对比**：

| 方案 | 多跳查询性能 | 时序关系建模便利性 | 学习成本 | 结论 |
|---|---|---|---|---|
| 关系数据库（MySQL/PostgreSQL） | 差，多次JOIN | 需额外设计时间表 | 低 | 不适合核心查询模式 |
| 文档存储（MongoDB） | 中，需应用层关联 | 中 | 低 | 适合存原文，不适合关系遍历 |
| 图数据库（Neo4j） | 好，原生图遍历 | 好，边属性天然支持时间区间 | 中 | 当前选择 |

**选定方案**：图数据库（开发期 NetworkX，演示/生产期 Neo4j）。

**验证实验**：构造50节点、3层关联深度的测试图，对比 Neo4j Cypher 查询与等价 SQL 多表 JOIN 在响应时间上的差异。符合预期：图查询响应时间应有数量级优势，且随跳数增加差距应进一步拉大。

**符合预期 →** MVP阶段用 NetworkX 验证逻辑正确性，演示阶段切换 Neo4j 验证性能优势。

**不符合预期 → 诊断**：若小规模数据下差距不明显（图优势通常在数据量大、跳数深时才显著），说明MVP阶段规模下关系数据库也能满足，可延后图数据库的工程投入。

**备用方案**：数据规模较小时，用关系数据库+应用层递归查询模拟图遍历，作为知识图谱层的简化实现，待规模增长后再迁移。

**进阶方案**：引入图数据库原生算法（PageRank、社区发现）做事件影响力排序或公司风险聚类分析，进一步挖掘图结构信息的价值。

---

### 10.7 双时态建模 vs 简单状态覆盖

**第一性原理**：金融舆情场景的核心需求之一是可追溯性——分析师需要回答"某个时间点市场已知道什么信息"，这是该领域的合规性基本要求（类比审计要求保留历史记录而非仅保留最新状态），而非锦上添花的功能。简单覆盖式更新会永久丢失这种可追溯能力，与该硬性要求直接冲突。

**备选方案对比**：

| 方案 | 历史可追溯性 | 存储成本 | 实现复杂度 | 结论 |
|---|---|---|---|---|
| 简单覆盖（只存最新状态） | 无 | 低 | 低 | 不满足可追溯性硬需求 |
| 双时态建模（t_valid/t_invalid） | 完整 | 中，每次更新新增边而非覆盖 | 中 | 当前选择 |

**选定方案**：双时态建模，可追溯性是基本要求而非可选项。

**验证实验**：模拟一个事件从首发到反转的完整生命周期（5次状态更新），验证查询任意历史时间点的已知状态能否准确还原。符合预期：100%准确还原每个历史时间点状态。

**符合预期 →** 标准流程固化。

**不符合预期 → 诊断**：检查边的失效逻辑（t_invalid设置时机）是否有遗漏场景，比如并发更新导致的边覆盖错误。

**备用方案**：若双时态实现复杂度超出团队 Neo4j 经验导致进度风险，降级为在 Event 节点上记录一个 `history` 字段（JSON数组，追加写），牺牲部分查询效率换取实现简单性，作为MVP阶段临时方案。

**进阶方案**：双时态数据积累后支撑更复杂时序分析，如"可信度从首次报道到官方确认平均需要多长时间"，反哺预警时效性优化。

---

### 10.8 预警机制：可信度×严重性二维矩阵 vs 单一可信度阈值

**第一性原理**：风险决策本质是两个独立变量的函数——"这件事有多大把握是真的"（认识论问题，对应可信度）与"这件事后果有多严重"（价值判断问题，对应严重性），这两个维度在数学上正交，类比金融领域期望损失=概率×损失幅度，二者不能合并为单一标量而不损失信息。这是此前讨论中已识别的设计缺陷，此处补充其第一性依据。

**备选方案对比**：

| 方案 | 信息完整性 | 决策可操作性 | 结论 |
|---|---|---|---|
| 单一可信度阈值映射风险等级 | 低，丢失严重性信息 | 简单但有误导性 | v1.0方案，已识别为缺陷 |
| 可信度×严重性二维矩阵 | 高 | 略复杂但更准确 | 当前选择 |

**选定方案**：二维矩阵，原理已在3.9.1节阐述。

**验证实验**：人工评审20个历史预警案例，由分析师对比二维矩阵分级与单一可信度分级哪个更符合直觉判断。符合预期阈值：二维矩阵分级更合理的案例占比 ≥ 70%。

**符合预期 →** 固化二维矩阵作为标准输出。

**不符合预期 → 诊断**：若存在反直觉案例，优先检查严重性基准表（3.9.1节）的事件类型分级是否需要调整，而非否定二维矩阵框架本身。

**备用方案**：若严重性评分难以标定（主观性争议大），简化为三档（高/中/低）而非连续分数，降低标定难度，矩阵框架保持不变。

**进阶方案**：积累分析师对预警准确性的反馈数据后，用反馈动态校准严重性基准表，形成闭环优化。

---

### 10.9 文档解析：MinerU+VLM路由 vs 纯OCR vs 纯VLM端到端

**第一性原理**：公告类PDF存在两种本质不同的输入形态——有文字层的可直接提取文本，无文字层（扫描件）必须做视觉理解，这是输入数据本身的二元性决定的客观事实，不是技术选型偏好。强行用单一工具处理两种形态必然在某一类上产生损失：纯OCR处理复杂版式扫描件效果不稳定，纯VLM处理有文字层PDF则是把简单问题复杂化、增加不必要的推理成本。

**备选方案对比**：

| 方案 | 处理有文字层PDF效率 | 处理扫描件能力 | 成本 | 结论 |
|---|---|---|---|---|
| 纯OCR | 中，不需要但兼容 | 中，复杂版式效果差 | 低 | 扫描件复杂版式不稳定 |
| 纯VLM端到端 | 低，大材小用 | 高 | 中高 | 对有文字层PDF是浪费 |
| 路由（有文字层→MinerU，扫描件→VLM） | 高 | 高 | 低，按需调用VLM | 当前选择 |

**选定方案**：路由策略，唯一同时尊重输入数据二元性又控制成本的方案。

**验证实验**：抽取50份混合公告PDF，对比路由方案与纯VLM方案在关键字段提取准确率和处理耗时上的差异。符合预期：路由方案准确率不低于纯VLM方案（±2%误差范围内），总处理耗时降低50%以上。

**符合预期 →** 固化路由逻辑。

**不符合预期 → 诊断**：若路由方案准确率明显低于纯VLM，说明MinerU对部分复杂版式（如多栏排版、嵌入图表的财务报表）处理不佳，定位具体触发场景。

**备用方案**：对MinerU解析失败或置信度低的文档，自动降级走VLM兜底，形成双重路由。

**进阶方案**：积累足够公告解析案例后，训练轻量级文档版式分类器，比简单的"是否有文字层"判断更精细地路由到最优解析工具。

---

### 10.10 数据获取：官方API vs 自建爬虫 vs 纯人工导入

**第一性原理**：数据获取层的稳定性直接决定系统可用性的下限——其他模块设计再精巧，数据源不稳定或不合规，整个系统就无法持续运行。自建爬虫面临目标网站结构变化导致的持续维护负担、以及合规风险两个根本性弱点，这是依赖第三方非授权接口的系统性脆弱性，不是技术能力问题；纯人工导入则完全不具备可持续运行的能力。

**备选方案对比**：

| 方案 | 稳定性 | 合规性 | 可持续运行能力 | 结论 |
|---|---|---|---|---|
| 自建爬虫 | 低，网站改版需持续维护 | 风险，可能违反ToS | 中 | 排除 |
| 纯人工导入 | 高 | 高 | 极低，无法规模化 | 仅作兜底 |
| 官方/合规API（akshare/tushare） | 较高，第三方维护接口稳定性 | 高，基于公开数据源封装 | 高，可定时自动运行 | 当前选择 |

**选定方案**：官方/合规API为主，人工导入为兜底。

**验证实验**：连续运行数据获取定时任务7天，统计接口可用率与数据完整性（是否有遗漏交易日）。符合预期阈值：接口可用率 ≥ 95%。

**符合预期 →** 进入常态化定时调度。

**不符合预期 → 诊断**：检查失败原因（限流、接口变更、网络问题），针对性增加重试和告警机制。

**备用方案**：可用率不达标时，引入多数据源冗余（akshare和tushare同时接入，互为备份），任一接口失败时自动切换。

**进阶方案**：接入更多数据源类型（研报、政策文件官方发布渠道）扩大覆盖面，并建立数据获取层健康监控仪表盘，主动发现接口异常。

---

## 附录：核心输出数据结构总览

```
article_event.json          # 单文章事件识别结果
event_cluster.json          # 同源事件簇
event_graph_edge.json       # 知识图谱边（双时态属性）
credibility_score.json      # 可信度评估结果
event_timeline.json         # 事件脉络时间线
alert_output.json           # 预警推送输出（含可信度×严重性矩阵）
```

所有模块之间通过 `article_id` 和 `event_cluster_id` 串联，支持任意模块独立更新而不影响其他模块，方便后续迭代优化。

---

*文档版本：v3.0 | 最后更新：2026-06-30*
